"""yodaed — the reproducible mission editor (plan: docs/mission-editor-plan.md).

Plain-text mission source in git; the resource fork is a build artifact.
Shipped surfaces so far: check (the question queue), graph, bits.
"""
